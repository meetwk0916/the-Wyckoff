# Crypto Provider Reports

Generated provider probe reports live here.

Do not commit generated `*.json` reports. Keep only this README so the directory boundary is visible.

Current default report path:

```text
crypto-workspace/reports/provider-probe-last.json
```
